# Define the jobs
# Author: Nuno Aguiar

jobs:
  ## ----------------------
  ## Job debug
  ## ----------------------

  # Start processing
  - name        : oJob Debug Args
    exec        : >
      log(stringify(args));
    