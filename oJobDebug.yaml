# Define the jobs
#
jobs:
  ## ----------------------
  ## Job debug
  ## ----------------------

  # Start processing
  - name        : oJob Debug Args
    exec        : >
      log(stringify(args));
    